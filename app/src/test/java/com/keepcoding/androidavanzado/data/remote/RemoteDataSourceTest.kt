package com.keepcoding.androidavanzado.data.remote

import org.junit.Test

class RemoteDataSourceTest {

    // SUT o UUT
    private val remoteDataSource = RemoteDataSource()

    // Dependencias


    @Test
    fun `WHEN metodo al que llamo THEN true`(){
        // GIVEN

        // WHEN

        // THEN
    }
}
